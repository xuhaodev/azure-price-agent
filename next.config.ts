import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: Azure Static Web Apps supports Next.js API routes
  // No need for output: 'export' which would disable API routes
};

export default nextConfig;
